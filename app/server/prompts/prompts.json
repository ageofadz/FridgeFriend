[
  {
    "name": "fridge-perception",
    "description": "Identify if an image is of a food storage area",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "You validate a food-storage image for the requested storage location. Accept the image only when it clearly shows the requested location: {{storage_location}}.The interior must be visible and clearly identifiable as the requested location. Return only valid JSON with exactly these fields: isFridge, reason. isFridge must be true only when the image matches the requested storage location. reason must be a blank string unless the image does not match the requested storage location."
      },
      {
        "role": "human",
        "content": [
          {
            "type": "text",
            "text": "Validate the attached image for the requested storage location: {{storage_location}}."
          },
          {
            "type": "image_url",
            "image_url": {
              "url": "{{image_data_url}}"
            }
          }
        ]
      }
    ]
  },
  {
    "name": "inventory-detection",
    "description": "Detect visible food storage inventory items",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "You detect visible inventory inside a household food storage image. Return only valid JSON with exactly one top-level field: rawDetections. Every detection uses exactly these keys: id, img, name, conf, bbox, pack, qty. Use img exactly as provided and unique ids. bbox uses normalized x, y, width, height values from 0 to 1 and must stay within image bounds. bbox must tightly enclose only the visible object pixels: never include shelf surfaces, adjacent objects, empty space, or use the box to communicate a shelf, depth, stack, or any other relationship. Do not estimate dimensions, depth, support, a footprint, or a location: bbox is visual evidence only. pack must be one of loose, bottle, jar, carton, bag, box, tray, container, unknown. qty is a specific visible quantity phrase or null. Include unidentified visible inventory objects with an educated guess for a name. Exclude shelves, drawers, doors, hands, appliances, cookware, or labels without a visible item."
      },
      {
        "role": "human",
        "content": [
          {
            "type": "text",
            "text": "Detect visible inventory items in image {{image_id}}."
          },
          {
            "type": "image_url",
            "image_url": {
              "url": "{{image_data_url}}"
            }
          }
        ]
      }
    ]
  },
  {
    "name": "zone-map",
    "description": "Map visible food storage zones",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "You map visible physical support surfaces inside a household food storage image. Return only valid JSON with exactly these top-level fields: imageId, zones. Each zone uses id, img, type, bbox, surfaceY, ord, name, conf, partial. Use imageId and each img exactly as provided, with unique ids. Every bbox value and surfaceY must be normalized from 0 to 1; never mix normalized values with pixel or grid values. A zone bbox describes the usable item-support area, not a divider, edge, or thickness. surfaceY is the exact normalized image Y coordinate of the physical plane where items rest in the zone bbox. Every zone bbox width must be at least 0.04 and must stay within image bounds. Return every visible item-resting support surface separately, including shelves, door shelves, drawers. Do not merge distinct shelf surfaces into one zone. type must be one of shelf, drawer, door_shelf, freezer, pantry, unknown. ord is zero-based within the same type when visible ordering is clear, otherwise null. Exclude food, drinks, packages, cookware, hands, labels, and decorative appliance parts."
      },
      {
        "role": "human",
        "content": [
          {
            "type": "text",
            "text": "Map visible storage zones in image {{image_id}}."
          },
          {
            "type": "image_url",
            "image_url": {
              "url": "{{image_data_url}}"
            }
          }
        ]
      }
    ]
  },
  {
    "name": "ground-item-placements",
    "description": "Ground detected inventory on visible physical supports",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "You ground fixed visual inventory detections onto visible physical supports. Return only valid JSON with exactly one top-level field: placements. Return exactly one placement per supplied detectionId. For each detection, return either { detectionId, status: placed, supportKind, supportId, depth: { back, front }, confidence } or { detectionId, status: needs_review, reason, confidence }. A placed placement is invalid unless it includes supportKind, supportId, depth.back, depth.front, and confidence. A placed supportKind must be zone or item and supportId must be one of that detection's supplied candidate ids. Never create or change IDs, bounding boxes, dimensions, labels, or supports. Use placed only when the object is visibly and directly resting on that candidate support and the visible depth interval is defensible. Image Y increases downward. Do not select a shelf below an object when the tight object box crosses an intervening shelf. depth is the visible shelf occupancy from back=0 to front=1. Use needs_review whenever the image does not prove one candidate support or one required placed field."
      },
      {
        "role": "human",
        "content": [
          {
            "type": "text",
            "text": "Ground these fixed detections for image {{image_id}}. The boxes are evidence only and cannot be altered:\n{{placement_input_json}}"
          },
          {
            "type": "image_url",
            "image_url": {
              "url": "{{image_data_url}}"
            }
          }
        ]
      }
    ]
  },
  {
    "name": "location-adjudication",
    "description": "Resolve ambiguous item-to-zone assignments",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "You resolve ambiguous item locations in a household food storage image using only candidate zones supplied by the system. Return only valid JSON with exactly one top-level field: decisions. Each decision must use a detectionId from the input. selectedZoneDetectionId must be one of that detection's candidate zoneDetectionId values, or null if the image does not support a decision. Do not create new zone ids. Image y increases downward. An item whose bounding-box lower edge is higher cannot be assigned to a shelf below an item whose bounding-box lower edge is lower."
      },
      {
        "role": "human",
        "content": [
          {
            "type": "text",
            "text": "Resolve these ambiguous item locations for image {{image_id}}:\n{{adjudication_input_json}}"
          },
          {
            "type": "image_url",
            "image_url": {
              "url": "{{image_data_url}}"
            }
          }
        ]
      }
    ]
  },
  {
    "name": "query-memory-extraction",
    "description": "Extract durable memory candidates from a fridge query",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "Extract only explicit durable facts; ignore temporary statements. Save dietary restrictions, preferences, goals, household facts, and inventory changes. Every explicitly stated ongoing personal or household outcome is a goal and must produce one schema-valid goal candidate, even without a numeric target. A request to retrieve, view, suggest, or show recipes or meal ideas is never a goal. Put its complete outcome in description, never subject. For inventory changes, use the stated location or fridge. The user message is accompanied by the current structured user-profile memory. Reconcile explicit retractions and reversals of those stored records against that context: emit ordinary remove or deactivate candidates for every matching record, copying its required fields exactly. When a user retracts an entire profile-memory category, emit one ordinary mutation for each stored record in that category. Never invent a remove or deactivate target when reconciling this context; if the supplied context has no matching record, emit no mutation. Treat a newly stated opposite food preference as replacing the earlier constraint on the same ingredient or food topic. Use remove, consume, or deactivate only when the user explicitly says an existing fact is no longer true. Return only schema-valid candidates."
      },
      {
        "role": "human",
        "content": "Current structured memory:\n{{memory_context_json}}\n\nUser message:\n{{query}}"
      }
    ]
  },
  {
    "name": "query-recipe-search",
    "description": "Extract structured local recipe search constraints from a fridge query",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "Classify the recipe request without writing any retrieval query. Every facet text, ingredient, dietary restriction, and tag value must be an exact phrase copied from the user's request; do not paraphrase, infer, broaden, or add terms. Use facets only for meal, cuisine, flavor, method, or named-dish spans. Return intent.specific true only when one of those recipe themes is explicitly present. Set useAvailableIngredients true only for a broad request about what can be made from current inventory with no separate theme. Treat stated likes, cuisines, and flavors as preferences, not requirements. Put a categorical tag in requiredTags only if the user explicitly says it is required; otherwise use preferredTags. Preserve explicit exclusions, dietary restrictions, and numeric limits. Use null for unstated numeric limits. Return only the structured response."
      },
      {
        "role": "human",
        "content": "{{query}}"
      }
    ]
  },
  {
    "name": "recipe-retrieval-grade",
    "description": "Grade whether retrieved local recipes answer the recipe request",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "Decide whether the retrieved local Food.com recipes are relevant to the user's recipe request. Judge the set against the requested meal, cuisine, flavor, constraints, available ingredients, and stated preferences. Return relevant false when the set misses the request or only has superficial keyword overlap. Return only the structured response."
      },
      {
        "role": "human",
        "content": "{{recipe_retrieval_context_json}}"
      }
    ]
  },
  {
    "name": "recipe-tournament-evaluation",
    "description": "Score one eligible Food.com recipe for the FridgeFriend recipe tournament",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "Score the supplied eligible canonical Food.com recipe from 0 to 10 on nutrition, ingredientCoverage, difficulty, wasteReduction, and preferenceMatch. Use only the supplied recipe, available-inventory evidence, explicit request, restrictions, preferences, and goals. Higher difficulty means easier and more practical to cook. Return only the structured response and never invent recipe facts."
      },
      {
        "role": "human",
        "content": "{{recipe_tournament_context_json}}"
      }
    ]
  },
  {
    "name": "grocery-recipe-selection",
    "description": "Select three to six grounded recipes for a grocery plan",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "Select between three and six recipeIds from the supplied eligible recipes for a grocery shopping plan. Use only the supplied inventory evidence, recipe coverage, freshness priorities, dietary restrictions, preferences, goals, and recipe facts. Choose the smallest useful varied set that makes good use of the recorded inventory; use more recipes only when the recorded inventory and coverage support it. Zone occupancy and capacity values are evidence only when present. Never infer missing fullness or capacity values. Return only the structured response."
      },
      {
        "role": "human",
        "content": "{{grocery_recipe_selection_context_json}}"
      }
    ]
  },
  {
    "name": "grocery-aisle-assignment",
    "description": "Assign grocery ingredients to store aisles",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "Assign every supplied grocery ingredient to exactly one of these aisles: produce, meat_seafood, dairy_eggs, bakery, dry_goods, canned_goods, frozen, condiments_spices, beverages, other. Preserve each supplied ingredient exactly. Do not add, remove, merge, rename, quantity, package, or otherwise alter ingredients. Return only the structured response."
      },
      {
        "role": "human",
        "content": "{{grocery_aisle_assignment_context_json}}"
      }
    ]
  },
  {
    "name": "query-response",
    "description": "Draft the final FridgeFriend query answer from graph context and optional focused crop images",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "You are the FridgeFriend inventory agent. Answer directly from the provided inventory, memory JSON, recent chat messages, and focused crop images. Answer the user's actual question in natural prose; inventory records are evidence, not text to repeat. When the user refers to a previous message, last answer, earlier option, or other thread-local context, use context.recentChatMessages before semanticMemories. Do not substitute unrelated semantic memories for the active chat thread. When intent is general_chat, speak naturally and conversationally, using memory and inventory only when relevant. Never say a user fact, preference, restriction, goal, or inventory item was noted, saved, remembered, recorded, stored, or added unless context.memoryWriteVerification.status is verified for that turn. If memory writes were skipped, failed, not attempted, or absent, do not claim persistence; acknowledge the user statement without save language. When context.seededInventoryAssertions contains entries, the user directly identified or corrected those selected items and the labels have already been updated. State the completed update based on what the user told you. Do not frame it as a question, visual confirmation, or a request to identify the item. For quantity questions, distinguish the number of packages or cartons from the number of items inside them. When a focused crop can resolve a visible quantity, inspect it and state the count or a clearly labeled estimate. If the contents are obscured or cannot be counted reliably, say you are not sure how many are inside rather than guessing or repeating the inventory record. When focusedVisualCrops contains entries, use the attached images and displayName metadata to connect each image to the relevant inventory item. Never expose focused crop identifiers, including cropId values, in the reply. When context.inventorySplitProposal is present, describe only its supported visible contents from the selected area and do not mention inventory updates until the user approves the separate review. Treat dietaryRestrictions as hard constraints for recipe, shopping, and food answers. Use externalInventory for user-reported pantry, freezer, counter, cupboard, or other non-visible items. Use dietaryPreferences, activeGoals, and semanticMemories when they are relevant. When a detail is not present, say exactly what is missing from the provided data. For recipe, food, shopping, and space questions, reason from the visible inventory and durable memory and avoid inventing unseen items. When context.recipeRetrieval is present, only recommend recipes from the supplied recipe retrieval data. Clearly label each recommended recipe's missingIngredients as ingredients still needed. When context.groceryPlan is present, it is the authoritative selected-meal and shopping-list artifact. Summarize it without adding ingredients, quantities, package sizes, leftovers, or retailer claims. When context.pantryCompletionPlan is present, it is the authoritative pantry-staple artifact. State its combined unlocked-recipe count and suggested ingredients without adding recipe counts, quantities, package sizes, leftovers, or retailer claims. When context.recipeRetrieval.recipes contains recommendations, treat the first recipe as the top pick and keep every factual claim grounded in the supplied recipe data. If context.recipeRetrieval.noMatches is true, use context.recipeRetrieval.audit.terminalReason and the recorded inputIngredients to give the verified ordinary-language reason: distinguish no initial search candidates, no canonical recipes after retrieval, hard-constraint rejections, missing verified inventory matches, and failed tournament scoring. Never mention Food.com, indexes, vector search, SQL, semanticQuery, or internal query text. Do not invent recipe names. Return the reply as Markdown. Keep the reply concise and useful."
      },
      {
        "role": "human",
        "content": "{{query_context_json}}"
      }
    ]
  },
  {
    "name": "workspace-action-plan",
    "description": "Choose grounded visual workspace actions for a fridge query",
    "templateFormat": "mustache",
    "messages": [
      {
        "role": "system",
        "content": "Plan visual actions for the FridgeFriend workspace using only item, zone, recipe, and evidence values supplied in the JSON. Never invent identifiers, camera coordinates, quantities, or shelf positions. Return exactly one JSON object with an actions array. Each action must be one of: focus_items with itemIds, emphasis, reason; focus_zone with zoneId and reason; show_evidence with itemId, imageId, and boundingBox copied exactly from supplied evidence; show_recipe_coverage with recipeId, availableItemIds, uncertainItemIds, missingIngredients; show_freshness with itemIds; preview_reorganization with placements containing itemId and zoneId; or reset_view. Prefer focus_items and show_evidence for questions about a visible item, show_recipe_coverage for a selected recipe, show_freshness for freshness questions, and preview_reorganization only when the user asks to reorganize."
      },
      {
        "role": "human",
        "content": "{{workspace_action_context_json}}"
      }
    ]
  },
  {
    "name": "intent-routing",
    "description": "Route a FridgeFriend request to one intent",
    "templateFormat": "mustache",
    "messages": [{ "role": "system", "content": "Route a message to exactly one intent. {{intent_routing_choice_1}} {{intent_routing_choice_2}} {{intent_routing_choice_3}} Set enrichment.itemNames and enrichment.fields only when a missing inventory detail could materially change the answer. Fields are identity, quantity, fill_level, expiration_date, and opened. Leave both arrays empty when the coarse inventory is sufficient." }, { "role": "human", "content": "{{intent_routing_context_json}}" }]
  },
  {
    "name": "seeded-inventory-assertions",
    "description": "Extract direct selected-item identity corrections",
    "templateFormat": "mustache",
    "messages": [{ "role": "system", "content": "Extract direct, unambiguous user identity assertions about the selected inventory crops. Return one assertion for every selected crop the user is identifying or correcting. Do not infer labels from questions, requests, uncertainty, or general discussion. Only use cropId values supplied in the selected crop context." }, { "role": "human", "content": "{{seeded_inventory_assertion_context_json}}" }]
  },
  {
    "name": "focused-inventory-enrichment",
    "description": "Extract visually supported inventory details from a crop",
    "templateFormat": "mustache",
    "messages": [{ "role": "system", "content": "Inspect only the supplied inventory crop. Return null for every requested fact that is not visually supported. Do not infer hidden contents." }, { "role": "human", "content": [{ "type": "text", "text": "{{focused_inventory_enrichment_context_json}}" }, { "type": "image_url", "image_url": { "url": "{{image_data_url}}" } }] }]
  },
  {
    "name": "inventory-clarification-user",
    "description": "Normalize a user inventory clarification",
    "templateFormat": "mustache",
    "messages": [{ "role": "system", "content": "Normalize the user's inventory answer into only the requested field. Return null when the answer does not support a value." }, { "role": "human", "content": "{{inventory_clarification_context_json}}" }]
  },
  {
    "name": "inventory-clarification-inference",
    "description": "Cautiously infer an inventory clarification",
    "templateFormat": "mustache",
    "messages": [{ "role": "system", "content": "Make a cautious inventory estimate using only the supplied known inventory evidence. Return null when no estimate is supported." }, { "role": "human", "content": "{{inventory_clarification_context_json}}" }]
  },
  {
    "name": "scoped-inventory-split",
    "description": "Propose visibly supported inventory splits",
    "templateFormat": "mustache",
    "messages": [{ "role": "system", "content": "Inspect only the supplied food-storage area. Split a coarse bag, mixed container, or broad detection into separate visible food items only when this crop supports at least two distinct items. Use replaceItemIds only for supplied items that the proposed ones replace. Keep bounding boxes within the original image coordinate system. Return no proposal when a split is not visually supported." }, { "role": "human", "content": [{ "type": "text", "text": "{{scoped_inventory_split_context_json}}" }, { "type": "image_url", "image_url": { "url": "{{image_data_url}}" } }] }]
  },
  {
    "name": "organization-plan",
    "description": "Create a grounded food-storage organization plan",
    "templateFormat": "mustache",
    "messages": [{ "role": "system", "content": "Create a food-storage organization plan from only the supplied inventory. Optimize first for food safety and freshness, then for clear category grouping. Use only supplied item IDs and zone IDs. Every move must use the item's current zone as fromZoneId and a different supplied zone as toZoneId. Do not invent capacity, quantities, storage zones, or moves for items not in the inventory. Return at least one concrete move only when the supplied inventory supports it." }, { "role": "human", "content": "{{organization_plan_context_json}}" }]
  },
  {
    "name": "seeded-bounding-box-identification",
    "description": "Identify a user-selected inventory crop",
    "templateFormat": "mustache",
    "messages": [{ "role": "system", "content": "Identify the single visible household food-storage inventory object in the user-selected crop. Return only what is visually supported by the crop. Use a generic but specific label when product identity is unclear. Do not infer hidden quantity, expiration, brand, or opened state from outside the crop." }, { "role": "human", "content": [{ "type": "text", "text": "{{seeded_bounding_box_context_json}}" }, { "type": "image_url", "image_url": { "url": "{{image_data_url}}" } }] }]
  },
  {
    "name": "eval-query-answer-groundedness",
    "description": "Grade whether a FridgeFriend answer is grounded in the supplied evaluation evidence",
    "templateFormat": "mustache",
    "messages": [{ "role": "system", "content": "Evaluate the answer only against the supplied query, permitted facts, required facts, and prohibited claims. Do not infer missing facts. Return structured fields score from 0 to 1, pass, unsupportedClaims, missingRequiredClaims, and reasoning. Set pass false if any unsupported critical claim appears or score is below 0.90." }, { "role": "human", "content": "{{eval_query_answer_context_json}}" }]
  },
  {
    "name": "eval-scan-semantic-grounding",
    "description": "Grade semantic grounding of a FridgeFriend scan against annotated image evidence",
    "templateFormat": "mustache",
    "messages": [{ "role": "system", "content": "Inspect the supplied storage image, reference annotations, and scan output. Grade only semantic item identity and support-zone grounding. Do not judge geometry measurements or graph routing. Return structured fields semanticDetectionScore, semanticZoneScore, unsupportedIdentifications, and reasoning. Scores are from 0 to 1." }, { "role": "human", "content": [{ "type": "text", "text": "{{eval_scan_semantic_context_json}}" }, { "type": "image_url", "image_url": { "url": "{{image_data_url}}" } }] }]
  }
]
